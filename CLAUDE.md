@AGENTS.md

# Claude Code 固有指示

この文書は Claude Code にだけ適用する。
共通ルールは毎セッション冒頭で import した `AGENTS.md` を正本として扱う。

## 方針

指示は短く、強く、曖昧さなく解釈する。
判断が競合する場合は、次の優先順位に従う。

1. 安全
2. 正しさ
3. 変更最小
4. 可読性
5. 速度

秘密情報、ユーザーの未コミット変更、Git 履歴を保護する。
依頼されていない変更を便乗させない。
単純な問題へ過剰な仕組みを導入しない。

## 計画モード

次のいずれかに該当する場合は、実装前に計画モードを開始する。

- 3 ステップ以上を要する。
- 仕様の解釈または設計判断が必要である。
- 影響範囲が不明である。
- 複数モジュールに及ぶ広範な変更である。

計画には調査、実装、検証を含める。
作業中に前提違い、想定外の差分、テスト失敗、権限不足が判明したら即停止する。
その状態のまま押し進めず、原因を整理して再計画する。
計画の更新は実際の進捗と一致させる。

## サブエージェント

調査、探索、独立した並列分析はサブエージェントへ分離する。
1 サブエージェントには 1 タスクだけを割り当てる。
編集範囲が重なる作業を同時に委譲しない。
最終判断、統合、差分確認、完了報告はメインエージェントが担う。

## 変更の進め方

変更前に既存実装、関連ルール、作業ツリーの状態を確認する。
既存の命名、モジュール境界、パターンを優先する。
変更は目的を達成する最小単位に限定する。
ユーザーの作業と競合する差分を見つけたら、上書きせずに停止する。
破壊的操作や外部への公開は、明示的な依頼がある場合だけ行う。

## 完了前検証

動作または整合性を証明せずに完了としてはならない。
変更範囲に対応するテスト、構文検証、バッチ起動、差分確認を実行する。
失敗を無視せず、原因を解消するか未解決事項として明示する。
実行できない検証があれば、理由と代替確認を報告する。
最後に `git status` と `git diff` で意図しない変更がないことを確認する。

## 設定ファイル

| ファイル | 用途 | Git 管理 |
|---|---|---|
| `.claude/settings.json` | 全開発者で共有するプロジェクト設定 | コミット対象 |
| `.claude/settings.local.json` | 権限、絶対パスなどローカル環境固有の設定 | gitignored |

Claude Code が共有設定へ権限を自動追加した場合は、コミット前に内容を確認する。
絶対パス、マシン固有コマンド、個人環境だけで必要な権限は
`.claude/settings.local.json` へ移動し、`.claude/settings.json` を共有可能な状態に戻す。
ローカル設定をコミットしない。

## レビューコマンド

| コマンド | 用途 |
|---|---|
| `/review` | 現在の変更差分を品質、回帰、検証の観点からレビューする |
| `/security-review` | 秘密情報、権限、入力処理、危険な操作をセキュリティ観点でレビューする |

大きな変更や公開前の変更では、通常レビューに加えて必要に応じてセキュリティレビューを行う。
